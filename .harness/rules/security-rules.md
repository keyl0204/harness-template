# 安全规则

## 强制要求

1. 权限模型必须表达为 actor、resource、action 和 condition。
2. 不要记录密钥、令牌或原始凭据。
3. 在信任边界校验外部输入。
4. 避免对用户可控值执行 shell 命令。
5. 读写文件时防止路径穿越。
6. API 响应中不要暴露内部异常细节。

## 触发安全审查的场景

- 认证或授权变更。
- 文件、网络或 shell 操作。
- 密钥处理。
- 支付、账单、管理员或破坏性操作。
