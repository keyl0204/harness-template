# Codex 规则

## 开始前

1. 读取 `AGENTS.md`。
2. 判断任务类型。
3. 读取匹配的 `.harness/workflows/` 文件。
4. 读取 `docs/` 下的相关项目文档。

## 工作中

- 优先做小而可审查的改动。
- 除非用户明确要求，不要回退用户改动。
- 保持 `.harness/` 和 `docs/` 的边界清晰。
- 行为、规则或架构变化时更新文档。

## 完成前

1. 先运行最小但有用的测试。
2. 可行时运行 `make check`。
3. 报告已运行命令和未解决风险。
