# 测试规则

## 强制要求

1. bugfix 必须补回归测试。
2. 新增 service 必须有单元测试。
3. 新增 API 必须有路由测试或集成测试。
4. 权限逻辑必须有越权访问测试。
5. 测试失败时不能标记任务完成。

## 命令

```bash
make test
make check
```

## 测试质量

- 测行为，不测实现细节。
- 测试命名应表达它保护的行为。
- 覆盖真实可能发生的失败模式。
