#!/usr/bin/env bash
set -euo pipefail

{% if project_type == "react" %}
make frontend-check
{% elif project_type in ["fastapi", "rag", "workflow", "multi-agent"] %}
make backend-check
{% else %}
echo "No check command configured for {{ project_type }}"
{% endif %}
