# AGENTS.md

## 项目说明

项目名称：{{ project_name }}

项目类型：{{ project_type }}

项目描述：{{ project_description }}

本项目使用 Codex 辅助开发。Codex 必须优先遵守 `.harness/` 下的工作规则，
再按任务需要读取 `docs/` 下的项目事实文档。

## 文件分区

- `.harness/`：AI 工程协作文件，包括 Agent、Skills、工作流、规则和状态。
- `docs/`：产品需求、领域规则、架构、运维和 ADR。
- `src/`：应用代码。
- `tests/`：验证代码。

## Harness 文件路由

{% if use_subagents %}
- 子 Agent 角色：`.harness/agents/`
- 子 Agent 模型配置：`.harness/config/models.yml`
{% endif %}
{% if use_skills %}
- Skills：`.harness/skills/`
{% endif %}
- 工作流：`.harness/workflows/`
- AI 规则：`.harness/rules/`
- 文档边界规则：`.harness/rules/doc-boundary-rules.md`
- 当前任务状态：`.harness/state/progress.md`
- 功能列表：`.harness/state/feature_list.json`

{% if use_skills %}
## 常用 Skills

- bugfix：`.harness/skills/bugfix/SKILL.md`
- docs-sync：`.harness/skills/docs-sync/SKILL.md`
- fastapi-api：`.harness/skills/fastapi-api/SKILL.md`
- react-page：`.harness/skills/react-page/SKILL.md`
- rag-pipeline：`.harness/skills/rag-pipeline/SKILL.md`
- workflow-orchestration：`.harness/skills/workflow-orchestration/SKILL.md`
- multi-agent-system：`.harness/skills/multi-agent-system/SKILL.md`
{% if use_security_rules %}
- security-review：`.harness/skills/security-review/SKILL.md`
{% endif %}
- test-writing：`.harness/skills/test-writing/SKILL.md`
- harness-review：`.harness/skills/harness-review/SKILL.md`

选择 Skill 时先读取对应 `SKILL.md` 的 frontmatter `description`，再按正文执行。

{% endif %}
## 项目文档路由

- 产品需求：`docs/product/prd.md`
- 用户故事：`docs/product/user-stories.md`
- 验收标准：`docs/product/acceptance-criteria.md`
- 术语表：`docs/domain/glossary.md`
- 业务规则：`docs/domain/business-rules.md`
- 状态机：`docs/domain/state-machines.md`
- 权限模型：`docs/domain/permission-model.md`
- 架构概览：`docs/architecture/overview.md`
- 模块边界：`docs/architecture/module-boundaries.md`
- API 规范：`docs/architecture/api-conventions.md`
- 数据库设计：`docs/architecture/database.md`
- 运维手册：`docs/operations/runbook.md`

## 工作原则

1. 修改代码前，先判断任务类型并读取对应 Harness 工作流。
2. 涉及业务行为时，读取相关 `docs/product/` 和 `docs/domain/` 文件。
3. 涉及架构、API 或数据库行为时，读取相关 `docs/architecture/` 文件。
{% if use_subagents %}
4. 使用子 Agent 时，优先以 `.harness/config/models.yml` 的 `model_profile` 映射决定模型和推理强度；`.harness/agents/*.md` 中的 `model` 和 `reasoning_effort` 作为兼容兜底。
5. 不要把产品需求写进 `.harness/`。
6. 不要把 Codex 工作规则写进 `docs/product/`。
7. 避免无关重构。
8. 避免不必要依赖。
9. 完成任务前必须运行验证。
10. 如果测试失败，继续修复，直到任务真正完成。
{% else %}
4. 未生成子 Agent 协议时，由当前 Codex 会话自行完成规划、实现、测试、审查和汇报。
5. 不要把产品需求写进 `.harness/`。
6. 不要把 Codex 工作规则写进 `docs/product/`。
7. 避免无关重构。
8. 避免不必要依赖。
9. 完成任务前必须运行验证。
10. 如果测试失败，继续修复，直到任务真正完成。
{% endif %}

## 常用命令

{% if project_type in ["fastapi", "rag", "workflow", "multi-agent"] %}
- Python 代码检查：`make lint`
- Python 类型检查：`make typecheck`
- Python 测试：`make test`
{% endif %}
{% if project_type == "react" %}
- 前端检查：`make frontend-check`
{% endif %}
- 完整验证：`make check`

## 完成标准

完成前运行：

```bash
make check
```

如果失败，先诊断并修复，再报告完成。
