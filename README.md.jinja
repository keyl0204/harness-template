# {{ project_name }}

{{ project_description }}

## 项目类型

`{{ project_type }}`

## 快速开始

```bash
make setup
make dev
```

## 从模板生成到当前文件夹

本项目由 Harness Copier 模板生成。生成到当前文件夹时，命令仍然必须包含两个位置参数：

```text
copier copy [选项] 模板源 目标目录
```

PowerShell 示例：

```powershell
uvx copier copy --trust --defaults `
  -d project_name={{ project_slug }} `
  -d project_type={{ project_type }} `
  -d package_manager={{ package_manager }} `
  -d frontend_package_manager={{ frontend_package_manager }} `
  -d use_docker={{ use_docker | lower }} `
  https://github.com/keyl0204/harness-template.git `
  .
```

如果目标目录已存在并且确认要覆盖测试，增加 `--force`：

```powershell
uvx copier copy --trust --defaults --force `
  -d project_name={{ project_slug }} `
  -d project_type={{ project_type }} `
  -d package_manager={{ package_manager }} `
  -d frontend_package_manager={{ frontend_package_manager }} `
  -d use_docker={{ use_docker | lower }} `
  https://github.com/keyl0204/harness-template.git `
  .
```

PowerShell 每行末尾的反引号后面不能有空格。缺少模板源和目标目录时，Copier 会报 `Expected at least 2 positional arguments`。

如果模板目录已经是 Git 仓库并且存在 tag，Copier 默认可能会使用最新 tag。验证本地最新提交时可以加：

```powershell
--vcs-ref=HEAD
```

## Harness

本项目内置 Harness 工程结构：

- `AGENTS.md`：Codex 入口和文档路由地图
{% if use_subagents %}
- `.harness/agents/`：子 Agent 角色协议
- `.harness/config/models.yml`：子 Agent 模型 profile 和兜底配置
{% endif %}
{% if use_skills %}
- `.harness/skills/`：可复用任务技能
{% endif %}
- `.harness/workflows/`：标准任务工作流
- `.harness/rules/`：AI 工程协作规则
- `.harness/state/`：任务状态和交接记录
- `.harness/scripts/harness-check.py`：Harness 引用、元数据和源码文件行数自检
- `docs/`：产品、领域、架构、运维和 ADR 文档

## 验证

```bash
make check
```

`make check` 会先运行 Harness 自检，再执行当前项目类型对应的后端或前端验证。

更多模板生成、Git 发布和升级命令见 `docs/operations/runbook.md`。
